# Azure AI Configuration
# Azure OpenAI endpoint (base URL without /openai suffix)
# Example: https://your-resource-name.cognitiveservices.azure.com
AZURE_AI_ENDPOINT=https://kunal-mhiogumv-eastus2.cognitiveservices.azure.com

# Your Azure AI API key (get from Azure portal: Models + Endpoints → Your Deployment → "Get endpoint")
AZURE_AI_KEY=your-api-key-here

# Your deployment name in Azure (this is what you named your deployment in Azure AI Foundry)
# Example: gpt-5-nano, gpt-4o, etc.
AZURE_AI_DEPLOYMENT=gpt-5-nano

# Azure API version (use the version shown in your deployment documentation)
AZURE_AI_API_VERSION=2024-12-01-preview

# Optional: Leave empty to use AZURE_AI_DEPLOYMENT as default
SEGMENTS_LLM_MODEL=

# Token limits (adjust based on your model's capacity)
SEGMENTS_MAX_CHARS=14000
SEGMENTS_COLLECTIVE_MAX_CHARS=16000
SEGMENTS_ITEMS_MAX_CHARS=16000
SEGMENTS_CHAPTERS_MAX_CHARS=20000

